import { FeedbackForm, Logo, PageHeading, Pattern } from "@/components";

export default function Header() {
  return (
    <header>
      <Pattern />
      <Logo />
      <PageHeading />
      <FeedbackForm />
    </header>
  );
}
